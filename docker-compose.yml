version: '3.8'

services:
  backend:
    build:
      context: ./core
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    environment:
      - DATABASE_URL=sqlite:///./vi_sense.db
      - SECRET_KEY=your-secret-key-here
    volumes:
      - ./core:/app
    depends_on:
      - db

 